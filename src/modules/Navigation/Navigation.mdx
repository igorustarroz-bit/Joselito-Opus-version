import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './Navigation.stories';

<Meta of={Stories} />

# Navigation (módulo)

## Intro

Cabecera (fiel al master): enlaces (Body/02) a la izquierda, **logo centrado** y utilidades a la derecha (Buscar con icono, Cuenta y Cesta en acento).

## Demo

<Canvas of={Stories.Default} />

## Subtemas

Tema propio (`data-theme`).

<Canvas of={Stories.Oscuro} />

## Comportamiento responsive

Los enlaces se ocultan por debajo de M (donde entra el módulo Menu). El logo queda centrado.

## Tokens

`type-body-2`, `--text-base`, `--text-accent-base`; logo con `currentColor`.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

BrandLogo, Icon; Menu, SecondaryMenu, Breadcrumb.
