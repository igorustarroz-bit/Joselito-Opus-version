import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './Footer.stories';

<Meta of={Stories} />

# Footer (módulo)

## Intro

Navigation / Footer (fiel al master): tres zonas — columnas de enlaces, banda central (newsletter + isotipo + columnas + redes) y barra inferior (copyright · legales · idioma).

## Demo

<Canvas of={Stories.Default} />

## Subtemas

Tema propio (`data-theme`); claro por defecto (como el master).

<Canvas of={Stories.Oscuro} />

## Tokens

Encabezados `type-body-2`, enlaces `type-body-3` (`--text-neutral-1`), título newsletter `type-title-3`, bordes `--stroke-neutral-4`.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

BrandLogo, Icon, Title.
