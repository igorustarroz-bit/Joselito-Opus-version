import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './CardsShowcase.stories';

<Meta of={Stories} />

# CardsShowcase (módulo)

## Intro

Escaparate (Cards / Showcase): un destacado grande + una lista de apoyo (7+5 columnas).

## Demo

<Canvas of={Stories.Default} />

## Comportamiento responsive

En móvil se apila; desde M el destacado ocupa 7 columnas y la lista 5.

## Tokens

`.grid-12`, AspectRatio, `type-title-*`, colores semánticos.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

AspectRatio, Title, ActionLink; CardLink.
