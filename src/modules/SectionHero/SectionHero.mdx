import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './SectionHero.stories';

<Meta of={Stories} />

# SectionHero (módulo)

## Intro

Hero de sección (Hero / Section hero) fiel al master de Figma: sección a 100% del viewport con fondo `--bg-base` y contenido **centrado** — eyebrow + título.

## Demo

<Canvas of={Stories.Default} />

## Anatomía

Bloque centrado (ancho ~540px): eyebrow con `type-body-6` + título con `type-title-4`.

## Subtemas

Tema propio (`data-theme`); por defecto el de marca (rojo).

<Canvas of={Stories.Blanco} />

## Tokens

Fondo `--bg-base`, texto `--text-base`; `type-body-6` (eyebrow) y `type-title-4` (título).

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

Title; Section header, Homepage hero.
