import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './ContentText.stories';

<Meta of={Stories} />

# ContentText (módulo)

## Intro

Content / Text only (fiel al master): etiqueta + título + cuerpo, con tres variantes.

## Demo

<Canvas of={Stories.Split} />

## Variantes

`split` (título izquierda / cuerpo derecha), `1-column` (apilado) y `2-column` (cuerpo a dos columnas).

<Canvas of={Stories.UnaColumna} />
<Canvas of={Stories.DosColumnas} />

## Tokens

`type-body-6` (label), `type-title-4` (título), `type-body-5` (cuerpo).

## Propiedades

<Controls of={Stories.Split} />

## Componentes relacionados

Title; ContentTitle, ContentIntro.
