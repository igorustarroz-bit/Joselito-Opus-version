import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './ListNumbers.stories';

<Meta of={Stories} />

# ListNumbers (módulo)

## Intro

List / Numbers (fiel al master): cabecera + filas con un valor grande (Title/05, serif) y una descripción (Body/05), con filete superior.

## Demo

<Canvas of={Stories.Default} />

## Tokens

Valor `type-title-5`; descripción `type-body-5`; filete `--stroke-neutral-2`; cabecera `type-title-1`.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

List/Characteristics, List/Timeline.
