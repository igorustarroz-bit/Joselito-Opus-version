import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './ContentTextImage.stories';

<Meta of={Stories} />

# ContentTextImage (módulo)

## Intro

Content / Text + Image (fiel al master): imagen + bloque de texto (label, título, cuerpo, botón secundario).

## Demo

<Canvas of={Stories.Left} />

## Variantes

`left` / `right` (7/5) y `half` (6/6). En móvil se apila.

<Canvas of={Stories.Right} />
<Canvas of={Stories.Half} />

## Tokens

`type-body-3` (label `--text-neutral-2` / cuerpo `--text-neutral-1`), `type-title-3`; borde `--stroke-neutral-4`; botón secundario.

## Propiedades

<Controls of={Stories.Left} />

## Componentes relacionados

Title, Button, AspectRatio.
