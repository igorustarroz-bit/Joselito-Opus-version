import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './Breadcrumb.stories';

<Meta of={Stories} />

# Breadcrumb (módulo)

## Intro

Navigation / Breadcrumb (fiel al master): banda de 80px con un enlace de vuelta (ArrowLeft + etiqueta).

## Demo

<Canvas of={Stories.Default} />

## Tokens

`type-body-3`, icono ArrowLeft, fondo `--bg-base`.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

Icon; SectionHeader, PreviousNext.
