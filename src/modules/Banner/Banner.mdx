import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './Banner.stories';

<Meta of={Stories} />

# Banner (módulo)

## Intro

Banners / Section banner (fiel al master): cabecera (título + "ver todos") + bloque de medios a sangre con overlay y una tarjeta blanca superpuesta (título, texto, botón).

## Demo

<Canvas of={Stories.Default} />

## Tokens

Cabecera `type-body-4`/`type-body-3`; tarjeta `type-title-2` + `type-body-3`; overlay `--bg-overlay`; botón terciario.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

Title, Button, Icon, AspectRatio.
