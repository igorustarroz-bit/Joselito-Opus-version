import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './ContentTitle.stories';

<Meta of={Stories} />

# ContentTitle (módulo)

## Intro

Content / Title (fiel al master): etiqueta + fila con título grande a la izquierda y descripción (+ enlace) a la derecha en desktop.

## Demo

<Canvas of={Stories.Default} />

## Comportamiento responsive

En móvil se apila (label, título, descripción, enlace); desde M pasa a 6 + 5 columnas.

<Canvas of={Stories.SoloTitulo} />

## Tokens

`type-body-6` (label), `type-title-4` (título), `type-body-5` (descripción); enlace = ActionLink.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

Title, ActionLink; SectionHeader.
