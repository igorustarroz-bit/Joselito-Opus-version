import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './PreviousNext.stories';

<Meta of={Stories} />

# PreviousNext (módulo)

## Intro

Navigation / PreviousNext (fiel al master): botón-icono cuadrado (64px, con borde) + etiqueta (Body/06) a cada lado, con separador superior.

## Demo

<Canvas of={Stories.Default} />

## Tokens

Botón-icono 64px con borde `--stroke-neutral-3`; etiqueta `type-body-6`; `border-t`.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

Icon, ButtonIcon; Breadcrumb.
