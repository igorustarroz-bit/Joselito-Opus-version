import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './Menu.stories';

<Meta of={Stories} />

# Menu (módulo)

## Intro

Menu (fiel al master): mega-menú desplegable con columna de categorías (el activo en negro con flecha, el resto atenuados), imagen destacada y bloque promocional con enlace "descubre más". Se muestra bajo la Navigation.

## Demo

<Canvas of={Stories.Default} />

## Tokens

Enlaces `type-body-2` (`--text-neutral-3` inactivos / `--text-base` activo); promo `type-body-2`; enlace ActionLink.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

Navigation, ActionLink, AspectRatio, Icon.
