import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './CardsCategories.stories';

<Meta of={Stories} />

# CardsCategories (módulo)

## Intro

Mosaicos de categoría (Cards / Categories): imagen con título superpuesto y flecha, enlazables.

## Demo

<Canvas of={Stories.Default} />

## Comportamiento

Overlay que se intensifica en hover; la flecha se desplaza. Toda la tarjeta es un enlace.

## Tokens

Overlay `--bg-overlay`, texto `--text-always-white`, `type-title-3`, AspectRatio.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

AspectRatio, Icon, Title; CardLink.
