import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './ListCharacteristics.stories';

<Meta of={Stories} />

# ListCharacteristics (módulo)

## Intro

List / Characteristics (fiel al master): cabecera (Title/04) + rejilla de características con número (Body/06), título (Title/01) y texto (Body/05).

## Demo

<Canvas of={Stories.Default} />

## Tokens

Número `type-body-6` (`--text-neutral-1`), título `type-title-1`, texto `type-body-5`.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

Title; List/Numbers.
