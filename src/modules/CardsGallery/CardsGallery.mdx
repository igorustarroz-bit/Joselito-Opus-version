import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './CardsGallery.stories';

<Meta of={Stories} />

# CardsGallery (módulo)

## Intro

Galería de imágenes (Cards / Gallery) en rejilla responsive con pie opcional.

## Demo

<Canvas of={Stories.Default} />

## Comportamiento responsive

1 columna en móvil; 2–4 desde M/LG según `columns`.

## Tokens

`.grid-wrapper`, `--grid-gutter`, AspectRatio, `type-label-2`.

## Propiedades

<Controls of={Stories.Default} />

## Componentes relacionados

AspectRatio, Title; ContentImage.
